HAL v8.3.1 — Missing Runtime Patch

Upload these files/folders into the ROOT of the existing GitHub repository,
preserving their paths.

This patch restores:
- backend/app/main.py
- chat / quote / rates / evidence / knowledge / plans / transcription API routers
- backend/app/services/*

Do not create an extra ashlar-hal-2/ directory.

After upload, Railway should start with:
uvicorn backend.app.main:app --host 0.0.0.0 --port $PORT

Expected /health version:
8.3.1
